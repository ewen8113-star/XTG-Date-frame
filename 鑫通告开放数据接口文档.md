# 鑫通告开放数据接口文档（/open/v1）

供种子经纪运营结算后台读取经纪人、通告、报名签约明细及人员资料数据。所有接口均为**只读**。

- **正式环境 Base URL**：`https://jarvis.tong-gao.com/api`
- 完整地址示例：`https://jarvis.tong-gao.com/api/open/v1/brokers`

---

## 1. 鉴权

所有接口以 **API Key** 鉴权，key 由鑫通告管理员在管理后台创建后线下提供。

每个请求携带请求头：

```
X-API-Key: xtg_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

- key 缺失、错误或已被禁用时，返回 `{"code":401,"message":"身份未认证"}`（HTTP 200，以 body 中 code 为准）
- key 丢失无法找回，只能联系管理员删除重建
- key 被禁用/删除后**即时失效**

## 2. 通用约定

### 响应结构

```json
{
  "code": 200,
  "message": "成功",
  "errorId": null,
  "data": { ... }
}
```

- `code=200` 成功，其余为失败；失败时 `message` 为可读错误信息（参数错误、数据不存在等），`errorId` 可用于服务端排查
- 分页数据的 `data` 结构：

```json
{
  "total": 11,
  "pageCount": 6,
  "list": [ ... ]
}
```

### 数据格式

- **所有业务 ID（经纪人/通告/人员）为字符串**，如 `"1503934885240832000"`，避免超长数字精度丢失（少数小整数 ID 如报名记录 ID 为数字，在 JS 安全范围内）
- 时间格式：`yyyy-MM-dd HH:mm:ss`，**东八区（北京时间）**，无时区后缀
- 无值字段返回 `null`
- 状态字段统一输出「原始编码 + `*Text` 中文」双字段，编码含义长期稳定

### 分页参数（所有列表接口通用）

| 参数 | 类型 | 说明 |
|---|---|---|
| page | int | 页码，从 1 开始，默认 1 |
| pageSize | int | 每页数量，默认 20，最大 200 |

## 3. 状态编码字典

**实名状态 verifyStatus**：`0` 未认证 / `1` 认证中 / `2` 已认证 / `3` 认证失败

**账号状态 accountStatus**：`1` 正常 / `2` 限制发布 / `3` 临时封号 / `4` 永久封号（临时封号/限时限制到期后自动按正常返回）

**通告状态 status**：`20` 进行中 / `30` 已结束 / `40` 已取消（接口只含已发布通告，草稿与审核态不对外；无独立"已下架"状态，平台下架体现为已取消 + cancelReason）

**人员在通告中的状态 status**（字符串枚举）：

| 编码 | 中文 | 说明 |
|---|---|---|
| SIGNED_UP | 已报名 | 已报名，未进入签约流程 |
| REJECTED | 未通过 | 报名被经纪标记不满意 |
| WAIT_SIGN | 待签约 | 已发起合约，人员未签署 |
| SIGNED | 已签约 | 已签署且未解约 |
| WITHDRAWN | 已撤回 | 人员拒签合约 |
| CANCELLED | 已解约 | 签约后解约完成 |

---

## 4. 接口列表

### 4.1 经纪人列表

`GET /open/v1/brokers`

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| page / pageSize | int | 否 | 分页 |
| updatedFrom | datetime | 否 | 只返回该时间之后新增或主档变化的数据，ISO 格式如 `2026-07-01T00:00:00` |

响应 `data.list[]`：

```json
{
  "brokerId": "1503934885240832000",
  "nickName": "王先生",
  "mobile": "18518759899",
  "bindMobile": "18518759899",
  "verifyStatus": 2,
  "verifyStatusText": "已认证",
  "accountStatus": 1,
  "accountStatusText": "正常",
  "registerTime": "2026-05-13 09:46:00",
  "lastLoginTime": "2026-07-14 10:21:00",
  "breachCount": 0,
  "briefingPublishedCount": 19,
  "briefingFinishedCount": 8,
  "signupRecordCount": 8,
  "signRecordCount": 3,
  "distinctSignupModelCount": 2,
  "distinctSignModelCount": 2,
  "updatedAt": "2026-07-14 10:21:00"
}
```

| 字段 | 说明 |
|---|---|
| brokerId | 经纪人唯一 ID，长期稳定 |
| mobile / bindMobile | 微信授权手机号 / 业务绑定手机号，可能为 null |
| breachCount | 当前有效违规次数（已成立举报计数，含其通告被举报），无违规为 0 |
| briefingPublishedCount / briefingFinishedCount | 累计发布 / 正常结束通告数 |
| signupRecordCount / signRecordCount | 累计报名 / 有效签约记录数（同一人跨通告重复计入；签约=已签署且未解约） |
| distinctSignupModelCount / distinctSignModelCount | 累计去重报名 / 签约人数（不同人员数） |
| updatedAt | **仅反映经纪人主档变更**，统计字段变化不触发（见第 5 节） |

### 4.2 经纪人详情

`GET /open/v1/brokers/{brokerId}`

响应 `data` 为单个经纪人对象，字段同 4.1。不存在时 code≠200，message 为"经纪人[xxx]不存在"。

### 4.3 通告列表

`GET /open/v1/briefings`

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| page / pageSize | int | 否 | 分页 |
| brokerId | string | 否 | 按发布经纪人过滤 |
| publishTimeFrom / publishTimeTo | datetime | 否 | 发布时间范围，ISO 格式 |
| updatedFrom | datetime | 否 | 只返回该时间之后新增或变化的数据 |

响应 `data.list[]`：

```json
{
  "briefingId": "1501847771615395840",
  "brokerId": "1503934885240832000",
  "title": "上海大酒店礼仪接待",
  "status": 30,
  "statusText": "已结束",
  "publishTime": "2026-07-18 15:26:00",
  "workDatetimeEnd": "2026-07-20 17:00:00",
  "cancelTime": null,
  "cancelReason": null,
  "signupRecordCount": 10,
  "signRecordCount": 3,
  "distinctSignupModelCount": 10,
  "distinctSignModelCount": 3,
  "updatedAt": "2026-07-20 17:01:00"
}
```

说明：
- `workDatetimeEnd`：工作结束时间。系统无独立"通告结束时间"字段，状态翻转为已结束以此为依据，审核"实际工作时间"请用此字段及详情中的 workDates/workTimes
- 报名有唯一约束（每人每通告一条），`signupRecordCount` 与 `distinctSignupModelCount` 同值
- `cancelReason` 涵盖主动取消、平台取消、举报成立取消的原因文字；系统无结构化"取消类型"字段

### 4.4 通告完整详情

`GET /open/v1/briefings/{briefingId}`

响应 `data` 在列表字段基础上追加（完整字段，非列表摘要）：

```json
{
  "publisherName": "王先生",
  "remark": "补充说明",
  "recruitType": "礼仪模特-礼仪",
  "genderRequire": 0,
  "peopleCount": 4,
  "workDateBegin": "2026-07-20",
  "workDateEnd": "2026-07-20",
  "workDates": ["2026-07-20"],
  "workTimes": [{ "begin": "15:00", "end": "17:00" }],
  "location": {
    "code": 310107,
    "areaName": "普陀区",
    "name": "上海大酒店（南京东路店）",
    "address": "上海市黄浦区南京东路xxx号",
    "userInput": "正门集合",
    "longitude": 121.47,
    "latitude": 31.23
  },
  "salary": {
    "type": 2, "unit": 2, "value": 600.0, "min": null, "max": null,
    "continuouslyType": null, "continuouslyUnit": null,
    "continuouslyValue": null, "continuouslyMin": null, "continuouslyMax": null,
    "hasOvertime": 1, "overtimeUnit": 1, "overtimeValue": 50.0,
    "otherPeriodSalary": null, "rewardDescription": null
  },
  "requireDescription": "活动现场礼仪接待，具体要求……",
  "requirement": {
    "age": { "min": 18, "max": 30 },
    "height": { "min": 165, "max": null },
    "weight": { "min": null, "max": null },
    "bust": { "min": null, "max": null },
    "waist": { "min": null, "max": null },
    "hips": { "min": null, "max": null },
    "shoulderWidth": { "min": null, "max": null },
    "hasTattoos": 2,
    "hairColor": null, "hairLength": null,
    "shoeSize": null, "clothesSize": null, "language": null
  },
  "stopTime": "2026-07-19 20:00:00",
  "isContinuouslyWorking": 0,
  "isPayDeposit": 1
}
```

字段说明：
- `genderRequire`：0 不限 / 1 男 / 2 女
- `salary.type`：1 面议 / 2 固定 / 3 区间；`salary.unit`：1 每小时 / 2 每天；`continuously*` 为连档薪资；`otherPeriodSalary` 为其他时段薪资原始 JSON 字符串
- `requirement` 中 Range 的 min/max 为 null 表示不限；`hasTattoos`：0 不限 / 1 有 / 2 无
- `stopTime`：报名截止时间；`isPayDeposit`：是否已支付保证金
- 发布时间（publishTime）、工作时间（workDates/workTimes）为独立字段，不混用

### 4.5 报名与签约人员明细

`GET /open/v1/participants`

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| page / pageSize | int | 否 | 分页 |
| briefingId | string | 三选一 | 按通告查 |
| brokerId | string | 三选一 | 按经纪人查其全部通告的明细（**历史合作判断用这个**） |
| modelUserId | string | 三选一 | 按人员查 |

三个过滤条件至少提供一个，可组合。按报名记录 ID 正序。

响应 `data.list[]`：

```json
{
  "recordId": 15,
  "briefingId": "1501847771615395840",
  "brokerId": "1503934885240832000",
  "modelUserId": "1340932330631004160",
  "displayName": "Mia",
  "mobile": "13800138000",
  "gender": 2,
  "age": 24,
  "status": "SIGNED",
  "statusText": "已签约",
  "signupTime": "2026-07-18 16:20:00",
  "signTime": "2026-07-19 11:05:00",
  "cancelTime": null,
  "cancelReason": null
}
```

说明：
- `modelUserId` 为人员唯一 ID，长期稳定，**判断"本季度首次合作"以此为准**（手机号可能变化，仅辅助核对）
- `gender`：1 男 / 2 女；`mobile` 优先业务绑定手机号，无则微信授权手机号
- `cancelTime` / `cancelReason`：解约完成时间及解约发起方填写的原因
- **本接口无 updatedAt / updatedFrom**（源表无更新时间列）。签约/解约变化实时反映在 `status` 字段上，请按 brokerId 或 briefingId 定期重新拉取全量

判断某人员是否与某经纪人首次合作：用 `brokerId + modelUserId` 查本接口，看是否存在更早的 `status=SIGNED`（或历史 CANCELLED，按你方口径）记录。

### 4.6 人员资料详情

`GET /open/v1/models/{modelUserId}`

响应 `data`：

```json
{
  "modelUserId": "1340932330631004160",
  "displayName": "Mia",
  "verifyStatus": 2,
  "verifyStatusText": "已认证",
  "accountStatus": 1,
  "accountStatusText": "正常",
  "registerTime": "2026-07-07 21:05:00",
  "lastLoginTime": "2026-07-12 20:40:00",
  "breachCount": 0,
  "avatarUrl": "https://img-....aliyuncs.com/M/avatar/....jpg",
  "photoUrls": [
    "https://img-....aliyuncs.com/M/....jpeg"
  ],
  "gender": 2,
  "age": 24,
  "birthday": "2002-01-01",
  "heightCm": 168,
  "weightKg": 50,
  "region": "普陀区",
  "bustCm": 84,
  "waistCm": 62,
  "hipsCm": 90,
  "shoulderWidthCm": 38,
  "shoeSize": "37",
  "clothingSize": "M",
  "hasTattoos": 2,
  "hairColor": "黑色",
  "hairLength": "长发",
  "languages": ["日语", "韩语"],
  "introduction": "个人资料介绍",
  "categoryTags": ["礼仪"],
  "updatedAt": "2026-07-14 10:30:00"
}
```

说明：
- `photoUrls` 为个人主页模卡照片（仅审核通过的图片）；`avatarUrl` / `photoUrls` 为**带签名的临时 URL，约 5 分钟有效**，请勿持久化保存 URL，需要展示时重新调用本接口获取当前有效地址
- 人员未填写资料时，体型等字段整体为 null；`hasTattoos`：0 不限 / 1 有 / 2 无
- 系统无"微信号"字段，不提供；无签约时点资料快照，返回当前资料
- 建议流程：从 4.5 明细取得 `modelUserId` → 调本接口自动同步资料与照片

---

## 5. 增量同步建议

- **经纪人**：`updatedFrom` 只覆盖主档变更（昵称、状态、封号等），**统计字段（发布数/报名数/签约数）变化不触发**。统计字段建议低频全量刷新（如每日一次），主档用 updatedFrom 增量
- **通告**：状态变化（结束、取消）会更新 updatedAt，`updatedFrom` 增量可靠，取消/举报成立均体现为 status=40 + cancelTime + cancelReason
- **报名/签约明细**：无增量参数，按活跃经纪人/通告定期重拉；同一条数据重复获取按 `recordId` 幂等覆盖即可
- 少量历史数据的 updatedAt 可能为 null（早期记录无更新时间），全量拉取时会返回，增量条件下不会命中，请以全量兜底

## 6. 不提供的数据

- **推荐关系**（需求 3.7）：系统无邀请码/上下级关系数据，不提供
- **业务事件流水**（需求 3.6）：不提供独立事件接口；取消/举报/解约/违规变化已包含在上述接口的状态、时间、原因字段中
