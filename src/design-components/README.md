# 设计组件库

这里存放尚未接入业务页面、可独立预览和复用的界面组件。

## 加载动画

- `loaders/DinoLoader.tsx`：React 组件。
- `loaders/DinoLoader.css`：组件样式，不依赖 `styled-components`。
- `loaders/preview.html`：独立视觉预览。

当前组件尚未在 `App.tsx` 或任何业务页面引用。确定替换位置后，再按具体场景接入。
