# 鑫通告种子经纪运营结算后台 UI 重设计 PRD

> 文档用途：交付 Stitch 进行后台管理系统 UI/UX 重设计  
> 产品范围：运营审核、种子经纪关系管理、奖励核算与财务付款  
> 文档版本：V1.0  
> 更新日期：2026-07-12

## 1. 项目背景

鑫通告正在执行第一期种子经纪激励计划。系统需要从鑫通告数据源导入经纪人、通告、签约名单与状态数据，并支持运营完成凭证核验、有效通告及有效签约审核、种子经纪关系维护、周度奖励结算和财务付款。

现有系统已覆盖主要业务功能，但页面仍以“功能和数据展示”为中心，关键任务分散在经纪人工作台的多个页签中。审核人员需要自行理解状态、查找证据、判断先后依赖；关系网络仅以列表呈现；奖励结果虽能展开查看，但缺少足够醒目的计算链路、风险校验和付款前复核机制。

本次不是简单更换颜色或组件样式，而是围绕高风险业务操作重新设计信息架构、任务流和反馈机制，使运营与财务能够快速、准确、可追溯地完成工作。

## 2. 产品目标

1. 让运营人员在一个连续工作流内完成“有效通告 + 有效签约”人工审核，减少遗漏、错判和重复操作。
2. 清晰展示种子经纪人的直接及多层上下级关系、身份来源、绑定状态和业绩贡献，避免关系归属混乱。
3. 让每一项奖励金额都能追溯到规则、周期、业务记录、审核结论及调整记录，并在付款前完成系统校验和人工复核。
4. 以“待办优先、异常优先、证据优先”的方式组织后台页面，降低运营学习成本。
5. 建立统一、克制、可信的运营后台视觉系统，支持高频扫描、比较和批量处理。
6. 同时提供亮色与暗色模式，并保证两套主题中的信息层级、状态语义和数据可读性一致。
7. 支持手机移动端处理核心业务，使运营和管理人员可随时查看待办、完成单条审核、查询关系与复核结算。

## 3. 核心用户与职责

| 角色 | 主要任务 | 最关注的信息 |
| --- | --- | --- |
| 运营 | 导入数据、审核通告与签约、维护身份和关系、生成预结算 | 待办数量、证据完整性、规则命中、异常与审核历史 |
| 客服 | 收集凭证、通知结果、登记异议、跟进补充材料 | 缺失材料、审核结果、无效原因、申诉时限 |
| 财务 | 复核付款单、确认付款、归档流水、月度对账 | 金额构成、复核状态、调整项、收款信息、付款记录 |
| 管理员 | 查看计划进展、风险、预算及人员网络 | 产出、达标率、预算消耗、关系规模、违规风险 |

第一阶段系统可继续不启用登录，但新设计必须预留角色权限、操作人和审核日志的展示位置。

## 4. 三个核心痛点

### 4.1 痛点一：有效通告与有效签约的人工审核步骤不清晰

**当前问题**

- 通告列表同时展示来源状态、凭证、有效通告、新增签约与人工审核状态，但没有明确告诉审核人“下一步该做什么”。
- 有效签约依赖有效通告先通过，但两个结果可在同一表单中独立选择，容易出现依赖关系不一致。
- 通告详情、录屏凭证、签约者资料、历史合作记录和审核动作分布在多个区域，视线移动距离长。
- “系统自动判定”和“人工审核结果”的视觉层级不够清楚，运营可能误改或误解自动判定项。
- 缺少面向审核队列的筛选、优先级、批量处理、快捷流转与完成度反馈。

**必须解决**

- 将审核设计为明确的步骤流：`资料完整性 -> 通告凭证 -> 有效通告结论 -> 签约人员核验 -> 有效签约结论 -> 复核提交`。
- 有效通告未通过时，系统自动锁定有效签约为不通过，并清楚说明依赖原因。
- 页面必须同时提供“系统判断依据、人工需核对项、最终结论”三个层级，不能只展示最终状态。
- 保存前展示结论摘要和影响金额；缺证、缺详情、原因未填写或状态冲突时禁止提交。
- 审核完成后自动进入下一条待审核记录，支持返回队列并保留筛选条件。

### 4.2 痛点二：种子经纪人的上下级关系网不可直观看清

**当前问题**

- 当前仅分开展示“我的上线”和“直接下线”，无法快速理解完整链路和多层裂变结构。
- 关系节点缺少绑定时间、来源、首季度有效期、达标状态、贡献奖励和风险状态。
- 通过手机号关联上下级时，缺少关系冲突、重复绑定、循环关系及权限资格的可视化校验。
- 解除关系属于高风险操作，但当前缺少影响范围预览、原因记录和审计历史。

**必须解决**

- 提供树状关系图与列表视图两种模式；默认以当前经纪人为中心，展示上线、当前节点、直接下线，并可逐层展开。
- 节点至少显示：昵称、唯一 ID、身份/期数、引荐权限、6+2 进度、首季度状态、有效通告、有效签约、风险状态。
- 点击节点可打开侧边详情，不丢失当前关系视图；支持快速切换“以该节点为中心”。
- 绑定前执行资格、唯一性、现有上线、循环关系和手机号匹配校验，并展示绑定影响确认。
- 关系变更必须记录生效时间、操作人、原因和历史版本；永久关系原则下不可直接无痕删除。

### 4.3 痛点三：奖励机制费用结算准确性缺少强保障

**当前问题**

- 奖励项虽可展开，但用户仍需自行核对数量、规则、来源记录和最终金额。
- 引荐基础奖是否已发过一次、引荐奖励是否仍在被引荐人首季度内，需要更明确的系统标识。
- 历史有效通告被取消后的抵扣、负数跨期结转、异议调整容易造成周期间金额理解困难。
- 当前付款订单主要保存在前端状态，缺少正式的生成、复核、锁定、调整、付款与归档状态链路。
- 财务确认付款前没有独立的完整性检查清单和双重确认。

**必须解决**

- 每个奖励项必须展示统一计算式：`合格数量 × 单价 ± 调整 = 金额`，并可下钻到原始业务记录。
- 结算总额必须展示：本期业务奖励、引荐奖励、抵扣/调整、上期结转、最终应付。
- 系统在提交前自动检查未完成审核、重复计奖、超过数量上限、基础奖重复发放、关系有效期、负数结转及金额汇总一致性。
- 付款单生成后锁定业务快照；后续修改必须通过调整单，保留修改前后数值、原因、证据和操作人。
- 财务页面必须同时展示计算结果与“复核清单”，所有必检项通过后才允许确认付款。

## 5. 业务规则基线

### 5.1 有效通告奖励

- 单价：1 元/条。
- 信息完整：通告内容、招聘类型、性别、人数、地址、时间、薪资等要素齐全。
- 外群凭证：完整录屏需展示群聊入口、群名称、发布时间、完整通告内容，微信群人数需不少于 300 人。
- 状态有效：未主动关闭或下架，无成立的虚假/违规举报。
- 同一经纪的通告若地址、核心档期、岗位类型重合度达到 70%，视为同一项目；单项目最多计 2 条。
- 单账号单日最多计 3 条，单周最多计 12 条。
- 已结算的历史有效通告若后续出现手动取消或举报取消，在发现周期抵扣 1 元。

### 5.2 有效签约奖励

- 单价：额外 2 元/条，不按签约人数计费。
- 前提：对应通告已通过有效通告审核。
- 签约人员主页资料完整，人员条件与通告要求匹配。
- 至少包含 1 名本季度首次与该经纪合作的人员；若全部为历史合作人员，则不计奖励。
- 新增身份优先按签约者用户 ID 判重，手机号等信息用于辅助核验。

### 5.3 引荐关系与奖励

- 种子经纪在季度内达到 6 条有效通告 + 2 条有效签约后解锁引荐权限。
- 上下级关系绑定后原则上永久保留，并支持多层裂变，但奖励以直接下线为核算对象。
- 基础达标奖：直接下线在入驻首季度达到 6+2、且已由运营确认晋升为种子经纪人后，奖励上线 10 元/人；每名直接下线仅发放一次，运营滞后操作时归入实际晋升所在周期。
- 成交增量奖：直接下线在入驻首季度每新增 1 条有效签约，奖励上线 1 元/条；多个直接下线分别按各自每日 3 条、每周 12 条上限独立计算后汇总，上线不另设合计上限。
- 被引荐人违规时，需支持取消对应引荐奖励及连带处罚记录。

### 5.4 周度结算节奏

| 时间 | 任务 | 系统状态 |
| --- | --- | --- |
| 周二 24:00 前 | 收集上一周期凭证 | 待提交/材料不全/已齐全 |
| 周三 | 运营完成通告与签约审核 | 待审核/审核中/有异议/已完成 |
| 周四 18:00 前 | 生成并发送预结算 | 预计算/待复核/已发送 |
| 周五 12:00 前 | 接收异议 | 无异议/申诉中 |
| 周五 17:00 前 | 完成简单异议复核 | 已修正/维持原判 |
| 周五 | 提交财务并付款 | 待财务复核/待付款/已付款 |
| 次周周一 18:00 前 | 复杂异议完成深度复核 | 转下期调整 |

## 6. 新信息架构

### 6.1 一级导航

1. **工作总览**：计划指标、今日待办、审核进度、结算进度、风险提醒。
2. **审核中心**：有效通告审核、有效签约审核、凭证异议、审核记录。
3. **经纪人管理**：全部经纪人、种子经纪人、晋升候选、个人工作台。
4. **关系网络**：关系图谱、关系绑定申请、关系变更记录、异常关系。
5. **奖励结算**：结算周期、预结算单、异议与调整、待提交财务。
6. **财务付款**：待复核、待付款、已付款、对账与归档。
7. **数据与规则**：运营统计、奖励预算、规则版本、导入记录。
8. **系统管理**：账号、角色、权限、操作日志。

### 6.2 全局能力

- 顶部全局搜索：支持昵称、手机号、经纪人 ID、通告 ID、结算单号搜索。
- 全局周期选择器：默认当前结算周，页面跳转后保持周期上下文。
- 通知中心：凭证逾期、待审核、金额异常、关系冲突、申诉截止提醒。
- 面包屑和返回路径：进入详情后能清楚返回原队列及原筛选状态。
- 主题切换：支持亮色、暗色和“跟随系统”三种模式，用户选择需在下次访问时保留。
- 跨端连续性：桌面端与移动端共享筛选、草稿和最近访问记录，切换设备后可继续处理。

## 7. 关键页面需求

### 7.1 工作总览

**目标**：打开系统即可知道今天必须处理什么，而不是先选择某个经纪人。

**页面结构**

- 顶部：当前结算周期、距关键截止时间、数据最近同步时间、立即同步按钮。
- 核心指标：待审通告、待审核签约、缺失凭证、凭证异议、待复核结算、待付款金额。
- 今日任务队列：按“已逾期、阻塞结算、高风险、普通”排序，点击直接进入对应工作流。
- 周度流程进度：凭证收集、人工审核、预结算、申诉、财务付款五阶段。
- 风险区：重复计奖、关系冲突、超限通告、负数结转、预算接近阈值。
- 计划概览：种子人数、解锁引荐人数、有效通告、有效签约、预算使用率。

### 7.2 审核中心列表

**目标**：形成统一审核队列，替代必须先进入单个经纪人工作台才能审核的路径。

**筛选项**

- 周期、审核类型、审核状态、材料状态、来源状态、风险标签、经纪人、是否影响当期结算。
- 快捷筛选：`我的待办`、`缺凭证`、`有异议`、`今日到期`、`系统自动不通过`。

**表格字段**

- 通告/通告 ID、经纪人、发布时间、材料完整度、有效通告状态、有效签约状态、签约人数、新增人数、风险标签、影响金额、下一步动作。

**交互要求**

- 支持多选，但仅允许对“同条件且低风险”的记录批量处理。
- 高风险、资料不完整或含签约人员的记录必须逐条审核。
- 每行直接显示下一步动作，如“匹配凭证”“核验签约者”“填写不通过原因”。
- 表头固定，支持列排序和用户自定义显示列；筛选条件可保存为视图。

### 7.3 单条审核工作台

**目标**：让审核人不离开当前页面即可完成全部判断。

**推荐布局**

- 顶部固定栏：返回队列、通告标题/ID、经纪人、当前步骤、影响金额、保存草稿、提交审核。
- 左侧主区：通告原始详情、工作要求、薪资、来源状态、规则命中提示。
- 中间证据区：录屏播放器、凭证列表、匹配状态、关键信息核对清单。
- 右侧固定审核栏：分步导航、每步结论、不通过原因、审核摘要。
- 签约核验阶段切换为人员列表，可查看用户 ID、手机号、资料完整度、匹配项、历史合作记录和新增/重复结论。

**六步审核流**

1. **资料完整性**：系统列出必填字段，缺失字段高亮；可触发重新导入。
2. **通告凭证**：选择并匹配录屏，逐项勾选群名、人数、时间、内容与完整录屏。
3. **有效通告结论**：展示项目合并、日上限、周上限、来源状态和举报情况；系统建议结论，人工确认。
4. **签约人员核验**：逐人显示资料完整度、岗位匹配、新旧身份及判重依据。
5. **有效签约结论**：仅在有效通告通过后开放；展示是否至少有一名合格新增人员。
6. **复核提交**：汇总两个结论、原因、证据、规则命中及预计奖励变化。

**状态与约束**

- 自动判定项使用“系统判定”标识并展示来源，不使用普通可编辑表单样式。
- 结论为不通过时必须选择结构化原因，可补充文字说明。
- “凭证异议”进入独立状态，不与普通待审核混在一起。
- 离开未保存页面时提示；支持自动保存草稿和审核用时记录。
- 提交成功后提供“下一条”主操作和“返回队列”次操作。

### 7.4 经纪人详情/工作台

**头部摘要**

- 身份、期数、唯一 ID、手机号、实名状态、账号风险、引荐权限、当前季度 6+2 进度。
- 关键动作：发起审核、查看关系、查看结算、调整身份；高风险操作收纳到更多菜单。

**页签**

- 概览、通告与审核、签约人员、关系网络、奖励结算、异议记录、操作日志。
- 页签右侧显示待处理数量，结算周期选择保持一致。

### 7.5 关系网络

**视图模式**

- 图谱视图：以当前经纪人为中心，上线置于上方，下线按层级向下展开；节点连线显示绑定时间或关系状态。
- 列表视图：适合查询、筛选和导出，字段包含层级、上线、下线、绑定时间、首季度截止日、达标进度、贡献奖励和风险。

**节点视觉**

- 身份用短标签区分：正式种子、引荐入驻、普通经纪人。
- 节点状态不能仅靠颜色，需配合文字/图标表达：已解锁、未达标、首季度已结束、违规冻结。
- 节点可显示 `有效通告 4/6`、`有效签约 1/2` 和当期贡献金额。

**关系绑定流程**

1. 输入手机号或 ID 搜索候选人。
2. 展示候选人身份、实名、现有上线、是否在首季度、风险记录。
3. 系统执行资格校验、重复关系校验、循环关系校验、唯一上线校验。
4. 展示绑定后的关系预览和奖励归属说明。
5. 填写关系依据/备注并二次确认。
6. 生成不可覆盖的关系变更记录。

**关系解除/修正**

- 不提供普通的“一键删除”。使用“申请关系修正”，要求填写原因并展示受影响的历史/未来奖励。
- 已进入结算的历史关系不回写覆盖，通过调整记录处理金额变化。

### 7.6 奖励结算中心

**周期列表**

- 展示周期状态、经纪人数、审核完成率、预估金额、异常数、申诉数、已付款金额。
- 只有审核完成率 100% 且阻塞异常为 0 时，才能进入“生成预结算”。

**单个经纪人结算页**

- 顶部金额摘要：本期业务奖励、引荐奖励、抵扣/调整、上期结转、最终应付。
- 五类奖励：有效通告、历史抵扣、有效签约、引荐基础达标、引荐成交增量。
- 每行统一展示：合格数量、单价、调整、金额、状态、异常标识、展开明细。
- 展开明细必须带原始记录链接，点击可回到对应通告审核或关系节点。
- 引荐基础奖明确显示“首次达标周”“是否已发放”“首季度截止日”。
- 引荐增量奖明确显示下线、通告、有效签约结论及关系生效日。

**计算与复核区**

```text
有效通告奖励 = 合格有效通告数 × 1 元
有效签约奖励 = 合格有效签约通告数 × 2 元
引荐基础奖 = 首季度首次达成 6+2 的直接下线数 × 10 元
引荐增量奖 = 首季度内直接下线新增有效签约数 × 1 元
本期小计 = 上述奖励 - 历史抵扣 ± 调整
最终应付 = 本期小计 + 上期负数结转
```

- 页面同时展示系统计算值与明细汇总值，两者不一致时阻断提交。
- 最终应付小于 0 时显示“结转下期”，不可生成负数付款。
- 生成预结算后记录规则版本、数据截止时间和审核快照。

### 7.7 异议与调整

- 异议必须关联具体通告、签约人员、关系或奖励项。
- 状态：待初检、简单复核、复杂复核、已修正、维持原判、已关闭。
- 展示申诉截止时间；同一对象同一奖励项仅允许一次正式申诉。
- 简单异议可修正当期；复杂异议标记“下期调整”。
- 调整单展示原金额、新金额、差额、原因、证据、复核人及关联结算周期。

### 7.8 财务付款

**待复核列表字段**

- 结算单号、周期、经纪人、收款手机号、业务奖励、引荐奖励、调整/结转、应付金额、运营复核人、异常状态。

**付款详情**

- 左侧为奖励构成和可下钻明细，右侧为固定复核清单。
- 必检项：审核全部完成、金额汇总一致、收款信息完整、无未决异议、调整已审批、无重复付款。
- “确认付款”前二次确认金额、收款人及备注；付款后录入/上传流水凭证。
- 已付款记录不可编辑，只能发起冲正或补差调整。

## 8. 状态体系

### 8.1 审核状态

`材料不全 -> 待审核 -> 审核中 -> 有异议/待补充 -> 已通过/不通过`

- 系统自动不通过作为独立来源标识，不与人工不通过混淆。
- 同一行同时展示“有效通告状态”和“有效签约状态”，另设整体任务状态。

### 8.2 结算状态

`草稿 -> 预计算 -> 待运营复核 -> 已发送预结算 -> 申诉中 -> 待财务复核 -> 待付款 -> 已付款 -> 已归档`

### 8.3 风险等级

- 高风险：重复付款、关系冲突、严重违规、金额不一致、证据疑似造假。
- 中风险：项目疑似重复、签约身份争议、复杂申诉、跨期调整。
- 提醒：缺字段、缺凭证、临近截止时间、收款信息不完整。

## 9. 视觉与交互设计要求（供 Stitch 使用）

### 9.1 整体气质

- 定位为专业、克制、高可信的运营与财务后台，不做营销落地页风格。
- 信息密度中高，强调扫描效率和层级秩序；减少大面积装饰、渐变、夸张圆角和无意义卡片。
- 页面主体使用明亮中性底色，深色文字；品牌色用于主操作和当前选中态。
- 成功、警告、危险、信息状态必须具有一致语义，并同时使用图标或文字，不能仅靠颜色。

### 9.2 布局与组件

- 桌面端优先设计宽度 1440px，同时保证 1280px 可用；后台最小宽度建议 1180px。
- 左侧导航固定，顶部上下文栏固定；长表格表头固定，关键操作在滚动时可见。
- 卡片圆角不超过 8px，不嵌套卡片；页面区域用留白、分割线和浅背景分组。
- 图标统一使用简洁线性图标；熟悉操作优先使用图标按钮并配 Tooltip。
- 表格支持紧凑/舒适密度切换、列显隐、排序、筛选、分页与空状态。
- 表单标签置于控件上方；必填、错误、帮助说明和禁用原因必须清楚。

### 9.3 关键交互反馈

- 所有异步动作提供处理中、成功、失败反馈，避免只出现一行临时文字。
- 高风险操作使用确认弹窗，明确对象、影响和不可逆结果。
- 自动保存显示最近保存时间；提交后使用明确的结果页/消息，不让用户猜测是否成功。
- 禁用按钮必须解释原因，例如“仍有 3 条通告未审核，暂不能生成结算”。
- 金额变化使用前后对比，负数和抵扣需显著但不制造恐慌。

### 9.4 UX 交互强化要求

#### 任务流与导航

- 任何核心任务从工作总览进入后，最多 3 次操作到达可执行状态，不让用户反复进入经纪人详情寻找入口。
- 审核、关系和结算均使用“列表/图谱 -> 详情 -> 处理结果”的稳定层级，返回后保留滚动位置、筛选、排序和已展开内容。
- 对步骤较长的任务使用清晰的步骤器；允许保存草稿，但不可跳过前置必检步骤直接提交。
- 页面刷新、浏览器返回及跨页签切换不得丢失已保存状态；未保存内容离开前必须提醒。
- 桌面端支持键盘效率操作：上下切换记录、打开详情、保存草稿、提交审核；快捷键需通过 Tooltip 或帮助菜单展示。

#### 审核交互

- 审核工作台支持“证据与原始数据并排比对”，滚动一侧时关键标题和结论栏保持可见。
- 视频凭证支持播放/暂停、倍速、前后跳转、全屏和时间点备注；审核结论可关联到具体视频时间点。
- 对通告字段、凭证内容和签约者资料的差异进行就地高亮，并允许一键定位下一处差异。
- 结构化核对项完成后实时更新步骤进度和结论建议，但系统建议不能替代人工确认。
- 不通过原因根据当前步骤动态提供常用选项；选择后仍可补充说明，减少重复输入。
- 提交前使用可扫描的审核摘要，不使用只包含“确定/取消”的空泛确认弹窗。

#### 列表与批量操作

- 筛选条件改变后即时展示结果数量；支持清空单项、清空全部、保存常用视图。
- 批量操作前展示适用记录、被排除记录及排除原因；执行后支持查看结果明细。
- 列表中的状态、异常和下一步动作保持在首屏可见，低优先级字段允许横向滚动或隐藏。
- 搜索无结果时展示当前筛选条件和可执行建议，而不是只显示“暂无数据”。

#### 关系图谱交互

- 支持拖拽画布、缩放、居中、展开/收起层级、搜索并定位节点；提供“回到当前经纪人”快捷操作。
- 节点悬停显示摘要，点击打开详情侧栏，双击或明确按钮切换为新的中心节点。
- 大型网络默认按需加载下层节点，并显示未展开的下线数量，避免一次渲染全部层级。
- 绑定前用关系路径预览明确展示“谁将成为谁的上线”，避免方向理解错误。
- 关系冲突直接高亮冲突节点和路径，并提供查看原绑定记录的入口。

#### 结算与金额交互

- 金额总览与奖励明细联动：点击某项金额自动定位并展开对应来源记录。
- 对计算公式提供“查看如何计算”，展示参与计算和被排除的数据，不只展示规则文字。
- 调整数值时实时预览本期应付、结转和下期影响，提交前展示修改前后对比。
- 付款确认采用分层确认：先完成复核清单，再核对收款人与金额，最后确认付款结果。
- 已提交或已付款数据保持只读；发起调整、补差或冲正时使用独立流程，不允许直接编辑历史值。

#### 反馈、容错与性能感知

- 轻量操作使用非阻塞式反馈；需要用户处理的错误在对应字段或记录附近显示。
- 网络失败时保留用户已输入内容，支持重试；不得因请求失败清空审核草稿。
- 耗时超过 1 秒的操作显示进行状态；超过 5 秒时说明正在处理的对象，避免用户重复点击。
- 导入、批量审核和生成结算等长任务显示进度、成功数、失败数，并允许查看失败原因。
- 删除、解除、付款等高风险动作不提供通用撤销假象，应通过审批、调整或冲正流程修正。

### 9.5 亮色与暗色模式

#### 主题机制

- 提供亮色、暗色、跟随系统三种选择；首次访问默认跟随操作系统。
- 主题切换入口放在全局顶部栏或用户菜单中，切换立即生效且不刷新页面。
- 用户主题偏好持久保存；跟随系统时能够响应设备主题变化。
- 设计稿必须以语义化设计令牌定义颜色，不允许在业务组件中散落主题专属色值。

#### 视觉要求

- 亮色模式使用中性浅色背景和清晰分隔，避免大面积纯白造成刺眼。
- 暗色模式使用深灰中性色层级，不使用纯黑铺满页面，也不将暗色简单处理为亮色反相。
- 两套主题均需定义：页面背景、区域背景、浮层、边框、主次文字、禁用态、悬停态、选中态、焦点态和遮罩。
- 成功、警告、危险、信息、待审核等状态在两套主题中保持相同语义，并满足对比度要求。
- 表格斑马纹、固定列阴影、图谱连线、金额正负值、视频播放器和图表需分别验证暗色表现。
- 图片、录屏缩略图及外部内容保持原始色彩，不进行主题反相；必要时增加中性边框以区分背景。
- 切换主题时避免出现白屏闪烁；加载阶段应尽早应用已保存主题。

### 9.6 移动端适配要求

#### 适配范围与原则

- 重点适配 360px-430px 宽度的手机竖屏，同时支持手机横屏；平板按响应式断点使用紧凑桌面布局。
- 移动端不是桌面端等比例缩小。保留高频核心任务，将复杂批量管理和大规模图谱浏览引导至桌面端。
- 移动端必须支持：查看总览与待办、筛选审核队列、完成单条审核、查看经纪人、查询关系、查看/复核结算、处理通知。
- 移动端可限制：复杂批量审核、关系批量维护、全量数据导出和系统权限配置，并明确提示使用桌面端。

#### 移动导航

- 使用底部导航承载 4-5 个最高频入口：总览、审核、经纪人、结算、更多。
- 二级页面使用顶部返回栏，清晰显示当前对象和关键状态；不同时保留桌面侧边栏。
- 筛选、排序和更多操作使用底部抽屉；抽屉内提供已选条件数量、重置和确认。
- 周期选择器保持在页面顶部上下文区，可横向滚动或使用底部选择器，不挤压标题。

#### 移动信息布局

- 桌面表格在移动端转为任务卡片或主从列表，每条优先显示对象、状态、风险、金额和下一步动作。
- 不在卡片中堆放全部字段；次要信息通过展开、详情页或底部抽屉查看。
- 金额汇总使用紧凑分组，最终应付保持醒目，但不得遮挡明细和操作。
- 长 ID、手机号和通告标题支持合理换行、截断与复制，任何文本不得溢出或覆盖其他内容。

#### 移动审核工作台

- 六步审核流改为纵向分步页面，每次聚焦一个任务；底部固定“上一步/下一步/提交”操作栏。
- 视频凭证在当前步骤顶部展示，支持横屏全屏播放；核对项置于视频下方，避免并排压缩。
- 签约人员使用逐人卡片核验，可左右切换或按列表展开；明确显示“第 N/M 人”和完成状态。
- 从外部页面返回后保留当前通告、步骤、视频时间点和草稿。
- 提交前使用全屏摘要页，集中展示结论、原因、证据和金额影响。

#### 移动关系与结算

- 关系网络默认使用层级列表/路径视图，提供轻量图谱入口；图谱支持双指缩放和拖动，但不能作为唯一查看方式。
- 当前经纪人的上线路径固定展示，直接下线按状态分组，可搜索并进入节点详情。
- 结算明细按奖励项折叠展示；点击金额展开来源记录，复核清单使用逐项勾选流程。
- 高风险付款操作需再次验证对象和金额，主按钮与危险按钮保持足够距离。

#### 触控与设备能力

- 主要触控目标不小于 44×44px，列表项之间保留足够间距，避免误触。
- 不依赖悬停展示必要信息；桌面 Tooltip 内容在移动端通过点击说明入口访问。
- 支持从手机相册/文件中选择并上传凭证；上传过程中显示进度并允许失败重试。
- 正确处理安全区域、软键盘、横竖屏切换和底部固定操作栏，输入框不得被键盘遮挡。

### 9.7 响应式断点建议

| 范围 | 布局策略 |
| --- | --- |
| `>= 1440px` | 完整桌面布局，多列审核工作台与完整关系图谱 |
| `1180px-1439px` | 紧凑桌面布局，减少辅助列，保留固定侧栏 |
| `768px-1179px` | 平板布局，可折叠导航，审核区改为双列或分步 |
| `< 768px` | 手机布局，底部导航、单列任务流、卡片化列表 |

### 9.8 可访问性

- 正文和关键数据满足 WCAG AA 对比度。
- 所有操作可通过键盘完成，焦点状态清晰。
- 图谱同时提供等价的列表视图；状态和金额不只依赖颜色表达。
- 中文正文建议不小于 14px，表格辅助文字不小于 12px。
- 支持浏览器文字放大至 200% 后完成核心任务，不出现内容遮挡或操作不可达。
- 尊重设备“减少动态效果”设置；动画不作为表达状态变化的唯一方式。

## 10. 核心异常与边界场景

1. 通告来源被手动/举报取消，但此前已通过并已结算：自动生成当前周期抵扣候选，人工确认后入账。
2. 有效通告不通过但有效签约被选为通过：系统强制纠正并解释依赖关系。
3. 签约者缺少用户 ID：标记身份信息不足，需人工补充或判为不合格，不静默使用姓名判重。
4. 同一人员手机号变化或多账号：展示潜在重复匹配和判重依据。
5. 通告命中同项目最多 2 条、单日 3 条或单周 12 条上限：明确指出被计奖记录和被排除记录。
6. 被引荐人已有上线：禁止重复绑定，展示原关系及修正申请入口。
7. 绑定关系将形成循环：禁止提交并可视化冲突路径。
8. 下线已超过入驻首季度：保留关系，但不再计算首季度引荐奖励。
9. 基础达标奖已经发放：再次达到条件不重复计奖，并展示原付款单。
10. 结算生成后审核结果发生变化：不直接改写已锁定快照，生成调整单。
11. 最终金额为 0：允许完成结算归档，不生成付款动作。
12. 最终金额为负：结转下期，付款金额为 0。
13. 存在未决申诉：对应奖励冻结，清晰区分无争议金额和冻结金额。
14. 数据导入失败或超过时效：展示最后成功同步时间，并阻断使用过期数据生成结算。
15. 用户在移动端审核过程中来电、锁屏或切换应用：已完成步骤和草稿自动保留。
16. 暗色模式下打开录屏或图片：媒体保持原色，控件与边界仍清晰可辨。
17. 手机网络不稳定导致提交超时：禁止重复生成审核或付款记录，并允许安全重试。
18. 移动端遇到仅支持桌面端的复杂操作：说明限制原因，并提供稍后处理标记，不展示无响应控件。

## 11. 数据与审计要求

- 所有业务对象使用唯一 ID 贯穿经纪人、通告、签约者、关系、结算和付款。
- 审核记录保存系统建议、人工结论、原因、操作人、时间、规则版本及关联证据。
- 关系记录保存上线、下线、生效时间、来源、操作人、修正原因和历史版本。
- 结算记录保存数据快照时间、奖励明细、计算公式、规则版本、审核状态、调整记录和付款流水。
- 关键列表支持导出，导出内容需带筛选条件、导出时间和操作人。
- 所有金额在数据层以“分”为最小单位存储与计算，界面统一格式化为人民币元，避免浮点误差。

## 12. 成功指标

| 指标 | 目标 |
| --- | --- |
| 单条普通通告平均审核时长 | 较现状下降 30% |
| 审核提交后因缺材料退回率 | 低于 5% |
| 有效通告与有效签约状态冲突 | 0 |
| 重复发放引荐基础奖 | 0 |
| 结算明细汇总与总额不一致 | 0 |
| 关系重复绑定或循环关系 | 0 |
| 财务付款前明细可追溯率 | 100% |
| 高风险操作审计记录覆盖率 | 100% |

## 13. 设计交付范围

Stitch 需至少输出以下桌面端高保真页面，并覆盖主要状态：

1. 工作总览。
2. 审核中心列表。
3. 单条审核工作台：通告凭证审核状态。
4. 单条审核工作台：签约人员核验状态。
5. 经纪人详情概览。
6. 关系网络图谱视图。
7. 关系绑定确认与冲突状态。
8. 结算周期列表。
9. 单个经纪人结算详情及奖励下钻。
10. 异议/调整详情。
11. 财务待复核列表与付款详情。
12. 已付款归档详情。

每个关键页面需同时提供桌面端和手机端设计，并覆盖亮色与暗色模式。至少需要完整展示：桌面亮色、桌面暗色、手机亮色、手机暗色四类关键场景。每个关键页面需提供正常态、空状态、加载态、错误态、权限不足态；审核与结算页面还需提供阻塞态和高风险异常态。

Stitch 还需交付：

- 响应式设计规则和关键断点说明。
- 亮色/暗色主题令牌与组件状态规范。
- 桌面表格转换为移动任务卡片的字段优先级规则。
- 审核、关系绑定、结算复核三个核心流程的交互原型。
- 加载、反馈、错误、确认、草稿恢复和跨端连续性的交互说明。

## 14. 验收标准

### 14.1 审核链路

- 用户可从总览待办进入审核队列，再进入单条审核，完成后连续处理下一条。
- 页面清晰区分系统判定、人工核对项和人工最终结论。
- 有效签约严格依赖有效通告，通过 UI 约束无法产生矛盾结果。
- 所有通过或不通过结论均可追溯到证据与结构化原因。

### 14.2 关系网络

- 任一经纪人的上线、直接下线和可展开的多层下线均可在同一视图理解。
- 绑定前可识别重复、唯一上线与循环关系冲突。
- 关系修正不会覆盖历史记录，且能说明奖励影响。

### 14.3 结算准确性

- 任一金额可从总额逐级下钻到奖励项、经纪人/下线、通告及审核记录。
- 基础达标奖只能结算一次；引荐奖励仅计算首季度有效期内数据。
- 已锁定结算不被后续业务修改静默覆盖。
- 未完成审核、未决高风险异常或金额不一致时无法提交财务。
- 财务确认付款前可完成全部复核项，并在付款后保留完整流水和审计记录。

### 14.4 主题与响应式体验

- 用户可在亮色、暗色和跟随系统之间切换，主题选择能够持久保存且无明显闪烁。
- 两套主题中的文字、状态、图表、表格、图谱和金额均满足可读性与对比度要求。
- 360px 宽手机竖屏下可完整完成单条通告与签约审核，不出现横向页面溢出或操作遮挡。
- 移动端可完成待办查看、关系查询、结算复核和通知处理；被限制的复杂操作有明确说明。
- 桌面与移动端切换后，已保存筛选、审核草稿和当前任务上下文可以恢复。
- 触控区域、软键盘、安全区域、横屏视频和弱网重试均符合第 9.6 节要求。

## 15. 非本期范围

- 经纪人端小程序重设计。
- 自动识别视频内容或使用 AI 替代人工审核。
- 微信红包/银行接口自动打款。
- 永久服务费分润体系的具体费率与结算逻辑。
- 多币种、税务发票和复杂会计凭证能力。

## 16. 给 Stitch 的执行提示

请基于本 PRD 设计“鑫通告种子经纪运营结算后台”的完整跨端体验。优先解决审核效率、关系可视化、金额可追溯性、移动端高频处理和跨端连续性，不要只做现有页面的视觉换肤。先建立统一的信息架构、响应式规则及亮色/暗色双主题设计系统，再按第 13 节输出关键页面和核心交互原型。设计时应使用真实的中文业务字段、合理的密集表格与移动任务卡片、明确的步骤状态和可下钻的结算明细；避免营销风格大标题、装饰性渐变、超大圆角、卡片嵌套和仅凭颜色表达状态。
